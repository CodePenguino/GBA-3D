This is a project made by CodePingu/CodePenguino.

NOTE: If you see a "compile_flags.txt" file lying around, unless you use vim with the clangd CoC.nvim extension, then you should be able to delete that. It's
just there for syntax highlighting in vim.